name: morsebound
description: Morsebound — adaptive sound-first Morse learning game by iCharles.
publish_to: "none"
version: 1.2.0+15

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.38.0
  audioplayers: ^6.8.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/morse/











