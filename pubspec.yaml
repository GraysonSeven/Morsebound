name: morsebound
description: Morsebound â€” adaptive sound-first Morse learning game by iCharles.
publish_to: "none"
version: 1.3.4+20

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.38.0
  audioplayers: ^6.8.1
  shared_preferences: ^2.5.5
  google_sign_in: 7.1.1
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7
  cloud_firestore: ^6.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/morse/














